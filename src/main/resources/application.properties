# ── Servidor ──
server.port=${PORT:8080}

# ── Supabase ──
spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.username=postgres.kyetgjeekssaidbxoncw
spring.datasource.password=${DB_PASSWORD:LashStudio#123}
spring.datasource.driver-class-name=org.postgresql.Driver

# ── HikariCP ──
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000

# ── JPA ──
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ── Jackson — serializar fechas como strings ISO (2026-03-25) ──
spring.jackson.serialization.write-dates-as-timestamps=false

# ── JWT ──
app.jwt.secret=lashStudioSecretKey2024SuperSeguraDeAlMenosTreintaYDosCaracteres
app.jwt.expiration=86400000

# ── CORS ──
app.cors.allowed-origins=https://lash-studio-tau.vercel.app,https://lash-studio-hykj.vercel.app,http://localhost:5173